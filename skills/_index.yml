generated: '2026-09-12'
method: generated
source: openapi/_original/goharbor-harbor-api-v2.0-swagger.yml, https://goharbor.io/docs/2.15.0/
provider: Harbor
providerId: goharbor
description: >-
  Packaged Agent Skills for the Harbor v2.0 REST API. Every operationId referenced in these
  skills was taken from Harbor's own published contract; none was invented. Each skill also
  carries the cross-cutting rules an agent needs and cannot infer from the spec — that
  Harbor has no idempotency key, that robot secrets are shown once and never recoverable,
  that deletion has no undo, and that the base URL is the operator's own instance.
skills:
- name: goharbor-scan-and-gate-artifact
  file: goharbor-scan-and-gate-artifact.md
  description: Scan a container artifact and decide whether to promote it from the vulnerability report.
  operations: [listArtifacts, getArtifact, scanArtifact, stopScanArtifact, getReportLog, getVulnerabilitiesAddition, getSecuritySummary]
- name: goharbor-provision-project-and-robot
  file: goharbor-provision-project-and-robot.md
  description: Create a project and issue a least-privilege robot account, then verify its effective permissions.
  operations: [listProjects, createProject, getProject, getProjectSummary, CreateRobot, ListRobot, GetRobotByID, RefreshSec, DeleteRobot, getCurrentUserPermissions, listQuotas]
- name: goharbor-subscribe-to-events
  file: goharbor-subscribe-to-events.md
  description: Subscribe to project events over webhooks in CloudEvents format and debug deliveries.
  operations: [GetSupportedEventTypes, CreateWebhookPolicyOfProject, ListWebhookPoliciesOfProject, GetWebhookPolicyOfProject, UpdateWebhookPolicyOfProject, DeleteWebhookPolicyOfProject, ListExecutionsOfWebhookPolicy, ListTasksOfWebhookExecution, GetLogsOfWebhookTask]
counts:
  skills: 3
provider_published_skills: false
provider_published_agents_md: false
note: Harbor publishes no skills/ directory and no AGENTS.md; these are generated by API Evangelist from the published contract and docs.
